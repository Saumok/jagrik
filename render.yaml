# Render Blueprint — free, no credit card. One web service serves the built SPA
# and the /api/* routes (server/index.ts serves dist/ in production).
# Deploy: push to GitHub → Render → New → Blueprint → pick this repo.
services:
  - type: web
    name: jagrik
    runtime: node
    plan: free
    region: singapore # closest free region to Kolkata; oregon/frankfurt also fine
    # --include=dev so vite/tsc are available for the build under NODE_ENV=production
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false # set in the Render dashboard (secret)
      - key: GEMINI_MODEL
        value: gemini-2.5-flash
      - key: GROQ_API_KEY
        sync: false
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: GMAIL_USER
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false
      - key: MAIL_FROM
        sync: false
      - key: DEMO_INBOX
        sync: false
      # Real email on Render (SMTP is blocked here) — Resend HTTP API
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM
        value: Jagrik <onboarding@resend.dev>
      # Durable storage — base64 of the Firebase service-account JSON
      - key: FIREBASE_SERVICE_ACCOUNT_B64
        sync: false
